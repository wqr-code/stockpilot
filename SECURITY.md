# 本地使用与数据范围

StockPilot 默认仅监听 127.0.0.1；设置 `STOCKPILOT_AUTH=1` 并连接数据库后，可启用邮箱密码登录和账号工作区隔离。该模式适合小规模项目演示，不包含邮箱验证、找回密码、管理员后台或企业级权限模型。

- 模型密钥仅在本地 `.env.demo` 或环境变量中配置，不返回前端。
- 本地模式的数据和单据保存在 `demo/data/workspace.local.json`；在线模式按账号保存在数据库中，这些文件和数据库内容都不随源码分发。
- AI 解读会把当前范围的统计摘要及选中商品证据发送到自行配置的模型服务。预测模型和补货计算在本机运行。
- AI Agent 只有选择商品、读取证据和生成解释的能力；确认需求、生产反馈和入库由用户通过表单完成。
- 配置模型后，进入经营分析页面可能自动产生付费调用；没有配置则仅运行本地功能。

公开仓库前检查提交文件，不上传 `.env.demo`、真实业务数据、日志或本地工作区备份。
